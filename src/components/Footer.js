import React from "react";

function Footer() {
	return (
		<div className="footer">
			<p>
				This is just an app that shows a little application of using state, props, and mapping through data fetched from an API to display
				on the frontEnd
			</p>
			<h4>All rights reserved @Micheal Dalu</h4>
		</div>
	);
}

export default Footer;
